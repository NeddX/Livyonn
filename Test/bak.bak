POP %RAX
POP %RBX

CALL @l2
RETURN

@l1:
    ;PUSH %RCX
    ;PINT
    CIGT %RCX, #0
    SUB %RCX, #1 
    CJMP @l1
    MOV %FNR, #1
    RETURN

@l2:
    CIGT %RAX, #0
    CJRP #3
    MOV %FNR, #1
    RETURN

    PUSH %RAX
    PINT
    SUB %RAX, #1
    JMP @l2
