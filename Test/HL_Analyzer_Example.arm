// Armavir, what if c# and typescript fucked
// High Level basic example
 
fn Entry(var argc, var argv)
{
    printline("10 - 6 * (3 + 2) is ", 10 - 6 * (3 + 2));
    capabilities();
}

fn capabilities() 
{
    var str = "declare dynamically typed variables like the toying languages";
    var num = 1;
    var dnum = 3.28180123;
    var bul = true;
    var undef; // Undefined dynamic variables

    string str2 = "but at the same time declare staticly typed variables like every other real programming language";
    int num2 = 2;
    double dnum2 = 6.192;
    bool bul2 = false;
    string undef2; // Undefined staticly typed variables

    // function calls! and comments too!!
    fnCall("very cool");
}

fn fnCall(var str)
{
    printline("not implemented yet apparently");
}
